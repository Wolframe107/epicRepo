#Libaries 
import numpy as np
import matplotlib.pyplot as plt
import cv2
from PIL import Image
import pylab

print("This script suggests break-points according to scene changes (only visual)")


cap = cv2.VideoCapture('.\test_video.mp4')

if cap.isOpened() == False:
    print("That's probably not the name of your clip")

"""
while True:
    filename = input("Please enter the filename including filetype (ex. test.mp4): ")
    cap = cv2.VideoCapture('test_video.mp4')

    if cap.isOpened() == False:
        print("That file doesn't exist :(")
    else:
        break
"""
print("yippie!")
